# 迭代记录

## v0.8 补丁十六：历史记录总折叠、折叠按钮左对齐与放大目录防遮挡（deepseek/test-feedback-fixes）

### 本次修复

- 修复 TEST-V08-038（折叠展开符号点击不起作用）：当前构建探针验证按钮、图标 svg 与放大态点击均正常，无法复现，症状指向 036 之前旧构建的双重折叠 bug——**请从 scheduler-ds-dev 目录运行 `npm run dev` 加载最新构建**。借本轮标题头重构把折叠判定区统一（40×40 按钮 + 表头整条热区、交互元素除外）并补回归断言。
- 修复 TEST-V08-039：历史记录标题头新增总折叠/展开按钮 `#collapseHistory`，接入统一折叠机制——折叠后隐藏日期目录、撤销条与记录列表，图标切换展开语义并同步 title/aria-label；折叠态下放大自动展开且状态一致（走完整折叠函数，图标/标题/aria 不再只改 aria）。
- 修复 TEST-V08-040：三个区块标题头统一为「折叠按钮 → 标题 → 其余操作」结构，标题右侧弹性留白（`margin-right: auto`）把放大、导出、新建、日期等操作推到最右；三个折叠按钮同为标题头首元素、x 坐标完全一致。
- 修复 TEST-V08-041：放大视图左侧日期目录去除 `position: sticky; top: 0` 残留，独占标题头下方的网格列并自带背景，条目过多时目录自身内滚；配合 037 的列表自滚动布局，任何滚动状态下日期条目都不会滑进吸顶标题头下面。
- 契约测试与 e2e 用例同步补齐（1 个新 e2e 用例 + 契约断言）。

### 验证

- `npm run verify` 通过：反馈文档校验、TypeScript、Vite 正式构建、构建产物同步、75 项 Node 测试和 85 项 Playwright 测试全部通过。

## v0.8 补丁十五：折叠按钮展开语义与历史日期表头吸顶（deepseek/test-feedback-fixes）

### 本次修复

- 修复 TEST-V08-036：长期描述与每日记录的折叠按钮在折叠后**切换为展开图标**（ChevronDown ↔ ChevronRight，同步 title/aria-label），替换原旋转样式。顺带修复图标替换引发的一个真实 bug：图标 svg 在点击处理中被移除后，事件冒泡到表头热区时 closest() 在脱离节点上失效导致双重折叠——按钮点击 stopPropagation，表头热区改用 composedPath 判定交互元素。
- 修复 TEST-V08-037：历史记录**展开态**的日期表头随滚动吸顶——普通视图贴在区块标题栏下方（多条记录逐条顶替），放大视图改为历史列表自身滚动、日期表头相对列表顶部吸顶；日期、进度与编辑/改期/导出/删除按钮浏览长记录时始终可见。
- 026 目录跳转断言改为测量历史列表滚动；契约与 e2e 用例补齐。

### 验证

- `npm run verify` 通过：反馈文档校验、TypeScript、Vite 正式构建、构建产物同步、75 项 Node 测试和 85 项 Playwright 测试全部通过。

## v0.8 补丁十四：SchedulerSaveTest 反馈批次（deepseek/test-feedback-fixes）

### 本次修复

- 修复 TEST-V08-029：Esc 分层退出。lightbox 暴露 isLightboxOpen 状态，工作区 Esc 守卫在图片放大遮罩打开时让位——第一次 Esc 只关遮罩，下一次 Esc 再关区块放大窗口。
- 修复 TEST-V08-030：长期描述与每日记录的折叠热区扩展到整个区块表头（标题、留白、保存状态等非交互区域均可折叠/展开），按钮、输入、下拉、标签保持原有行为，表头显示 pointer 光标。
- 修复 TEST-V08-031：新任务截止日期默认值新增 3 天后、7 天后、本周五（已过取下周五）、下周一（当天为周一时取下周一）四个选项，resolveDefaultDueDate 与迁移白名单同步扩展。
- 修复 TEST-V08-032：新增 video 附件类型（mp4/webm/ogg），附件面板可直接预览播放；Markdown 中的 attachment: 视频引用（图片或链接语法）渲染为带控件的内嵌播放器。
- 修复 TEST-V08-033：markdown 编辑器增加 toolbarExtras 扩展点，长期描述与每日记录的放大按钮移入编辑器工具栏、紧贴“源码/预览”切换键右侧。
- 修复 TEST-V08-034：放大模式下源码框自动铺展到窗口底部（height auto !important 覆盖手动 resize 的内联高度）并禁用手动 resize，退出放大恢复。
- 修复 TEST-V08-035：工作区三个区块标题栏（长期描述/每日记录/历史记录）在面板滚动与放大视图中 sticky 吸顶，滚动浏览长文档时标题与操作按钮始终可见。
- 契约、领域与 e2e 用例同步补齐（共 5 个新 e2e 用例 + 契约断言 + 领域日期用例）。

### 验证

- `npm run verify` 通过：反馈文档校验、TypeScript、Vite 正式构建、构建产物同步、75 项 Node 测试和 82 项 Playwright 测试全部通过。

## v0.8 补丁十三：放大层编辑器滚动修复（deepseek/test-feedback-fixes）

### 本次修复

- 修复 TEST-V08-028：放大每日记录/长期描述后，编译后的 md 文档区无法滚动查看页面外的上下内容。根因是放大层内 markdown-editor 为块级容器、内部 markdown-shell 未撑满剩余高度，源码/预览窗格只有约 280px 高，长内容被困在小窗格里。
- 放大层内 markdown-editor 改为纵向 flex 容器、markdown-shell 撑满剩余高度、markdown-fallback 与 markdown-source 取消最小高度限制：源码与编译预览窗格各自获得约全屏高度并独立内滚，长文档可完整滚动查看。
- 新增 e2e 回归（长记录放大后源码/预览窗格高度与滚动到底断言），契约测试覆盖四条放大层样式规则。

### 验证

- `npm run verify` 通过：反馈文档校验、TypeScript、Vite 正式构建、构建产物同步、73 项 Node 测试和 77 项 Playwright 测试全部通过。

## v0.8 补丁十二：Markdown 文档导出 PDF（deepseek/test-feedback-fixes）

### 本次修复

- 修复 TEST-V08-027：新增 `src/ui/pdf-export.ts`，采用**方案 A（浏览器原生打印）**——隐藏 iframe 中以 srcdoc 排版（A4、页边距、图片 max-width、表格/代码块/引用块跨页保护、标题不孤行），等待全部图片加载完成后调用 `window.print()`，用户在打印对话框选择“另存为 PDF”即可保存本地文件；文档标题作为 Chrome/Edge 保存对话框的默认文件名。
- 三个导出入口：**每日记录**编辑器工具栏、**每条历史记录**操作区、**长期描述**标题栏的“导出 PDF”按钮（导出前自动落盘）。
- Markdown 中的 `attachment:` 图片经渲染层解析为 blob URL，在导出文档中正常显示；失效附件显示占位说明。
- 契约测试覆盖 iframe/A4 打印样式/图片等待/三处入口；e2e stub `window.print` 断言三个入口各自生成打印文档、标题含“工作记录”、附件图片以 blob 地址加载成功。

### 验证

- `npm run verify` 通过：反馈文档校验、TypeScript、Vite 正式构建、构建产物同步、73 项 Node 测试和 76 项 Playwright 测试全部通过。

## v0.8 补丁十一：区块折叠、历史放大日期目录与折叠空白修复（deepseek/test-feedback-fixes）

### 本次修复

- 修复 TEST-V08-026：长期描述与每日记录标题栏新增**折叠按钮**（chevron 随状态旋转），点击收起区块内容只留标题，再点展开；放大已折叠区块时自动先展开保证视图完整。
- 历史记录放大视图改为**左侧日期目录 + 右侧记录列表**：目录按日期倒序展示日期与进度、高亮当前记录，点击条目 `scrollIntoView` 丝滑滚动跳转到对应记录；窄屏下目录折叠为顶部横向排列。
- 修复多条历史记录折叠后**条目间残留空白**：折叠态内容不再保留内边距（0fr 行高度归零，消除空白条），展开态由 responsive 规则补回内边距与 420px 上限。
- 新增 TEST-V08-026 e2e 回归（折叠开关、目录跳转、折叠几何零残留），更新契约测试并重采集视觉基线。

### 验证

- `npm run verify` 通过：反馈文档校验、TypeScript、Vite 正式构建、构建产物同步、72 项 Node 测试和 75 项 Playwright 测试全部通过。

## v0.8 补丁十：放大视图完整化与布局优化（deepseek/test-feedback-fixes）

### 本次修复

- 修复 TEST-V08-025：图片放大遮罩由只设 max-width/max-height 改为**撑满可用空间**（object-fit: contain，小图也放大到可读尺寸），新增顶部操作栏：**放大/缩小切换**（ZoomIn/ZoomOut 独立图标，点击图片或按钮在“适应窗口 ↔ 原始尺寸”间切换）与**关闭按钮**；点舞台空白、关闭按钮或 Esc 关闭。
- 分区放大按钮（长期描述/每日记录/历史记录）图标随状态切换：放大态显示 **Minimize2（缩小）**、恢复态显示 Maximize2（放大），标题与 aria 标签同步为“放大/恢复 ××”——不再共用放大图标。
- 历史记录放大时**自动展开全部条目**，展开态内容取消 420px 上限与内滚动，保证放大视图完整展示；放大层内容按约 1120px 行宽居中排版，优化阅读体验。
- `icon()` 输出补齐 `lucide` 与 `lucide-<name>` 类名（数字前正确断词），与静态图标路径一致，便于样式与测试选择。

### 验证

- `npm run verify` 通过：反馈文档校验、TypeScript、Vite 正式构建、构建产物同步、72 项 Node 测试和 74 项 Playwright 测试全部通过（扩展 017c/022，新增 025）。

## v0.8 补丁九：组件级放大、丝带对比度与历史记录交互返工（deepseek/test-feedback-fixes）

### 本次修复

- 返工 TEST-V08-022：按用户要求取消工作区全局放大，改为**长期描述、每日记录、历史记录三个组件各自的放大按钮**——点击后该组件全屏阅读（字号 16.5px、行距 1.75），Esc 或按钮恢复。修复放大按钮没有图标的问题：根因是 `maximize-2` 不在图标注册表中，图标注册表补上 `Maximize2` 与 `CalendarClock`。
- 返工 TEST-V08-023：丝带配色由青色改为**紫罗兰 #8b5cf6 + 白色投影描边**，与暖/冷优先级底色和便利签文件夹层都有明显对比。
- 新增 TEST-V08-024：历史记录点击日期行**展开/折叠**（chevron 随 220ms 高度动画旋转，展开另一条时旧条目平滑收起并切换到编辑器）；每条记录新增“修改记录日期”按钮，后端新增 `changeWorkLogDate` 把记录移动到目标日期（同任务同日期冲突报错，主记录换用新日期 ID，冲突副本保留 ID）；展开内容上限 420px 可滚动。
- 契约与 e2e 用例同步更新：组件放大/恢复、丝带出现消失、展开折叠与改期。

### 验证

- `npm run verify` 通过：反馈文档校验、TypeScript、Vite 正式构建、构建产物同步、72 项 Node 测试和 73 项 Playwright 测试全部通过。

## v0.8 补丁八：工作区放大阅读模式与近期活跃丝带标记（deepseek/test-feedback-fixes）

### 本次修复

- 修复 TEST-V08-022：任务工作区标题栏新增“放大工作区”按钮。阅读模式下工作区变成全屏覆盖层（任何视口生效），内容最大宽度 1080px 居中，Markdown 编辑器与历史记录字号加大、行距放宽，历史条目不再限高；再点按钮或按 Esc 恢复。会话内状态，不写入偏好。
- 修复 TEST-V08-023：近期有工作记录的任务卡片与文件夹标题左上角显示青绿色一角丝带（带 tooltip 与无障碍标签）；近期窗口在侧栏“新任务默认值”区可设置（今天/三天/一周/两周/一个月/关闭，默认一周）。后端新增 `listLatestWorklogDates` 可选能力，只读任务索引元数据即可判断活跃（不加载 Markdown 正文）；文件夹活跃 = 其任意后代任务活跃；工作记录保存/删除/恢复/导入合并后自动刷新标记。
- 契约测试覆盖偏好字段、渲染类、丝带样式与后端元数据能力；e2e 覆盖放大/恢复与丝带出现消失及设置切换；视觉基线按新标题栏重新采集。

### 验证

- `npm run verify` 通过：反馈文档校验、TypeScript、Vite 正式构建、构建产物同步、71 项 Node 测试和 72 项 Playwright 测试全部通过。

## v0.8 补丁七：文件拖到窗口任意位置不再被浏览器直接打开（deepseek/test-feedback-fixes）

### 本次修复

- 修复 TEST-V08-021：此前只有附件面板与 Markdown 编辑器处理文件拖放，把 PDF 拖到窗口其他位置会被浏览器默认行为直接导航打开。新增窗口级 dragover/drop 守卫：含文件的拖放一律阻止浏览器默认行为；未选中任务时提示“先选中一个任务，再把文件拖进来上传为附件”，选中任务后落点不在附件面板/编辑器内的文件统一经 uploadAttachments 入库为附件；面板与编辑器区域保持原有流程避免重复入库。
- 新增 e2e 回归用例：未选中任务时断言提示出现且页面不跳转，选中后拖到附件页以外断言附件落盘；契约测试覆盖全局守卫与 copy/none 光标逻辑。

### 验证

- `npm run verify` 通过：反馈文档校验、TypeScript、Vite 正式构建、构建产物同步、70 项 Node 测试和 70 项 Playwright 测试全部通过。

## v0.8 补丁六：附件面板拖拽上传与打开任务文件夹（deepseek/test-feedback-fixes）

### 本次修复

- 修复 TEST-V08-019：附件面板支持直接拖拽文件上传。`attachmentsPanel` 增加 dragenter/dragover/dragleave/drop 处理：拖入文件时面板高亮并显示 copy 光标，松手后与“添加附件”文件选择器共用 `uploadAttachments` 入库流程（20 MB 限制、逐项错误提示、刷新列表与空间统计），超限文件被明确拒绝。
- 修复 TEST-V08-020：附件工具栏新增“打开任务文件夹”按钮。纯 Web 应用无法直接呼出系统资源管理器，因此由本地目录后端新增的 `revealTaskDirectory` 可选能力用系统文件选择器（`showOpenFilePicker` + `startIn`）定位到任务目录（tasks/<任务ID>/），这是浏览器能力范围内的可行近似；IndexedDB 等无真实目录的后端明确提示不支持。
- 契约测试覆盖拖拽监听、dropEffect、drag-over 高亮、共用上传流程与后端可选能力；e2e 覆盖拖入入库与落盘内容一致、超限拒绝、系统选择器定位到任务目录。

### 验证

- `npm run verify` 通过：反馈文档校验、TypeScript、Vite 正式构建、构建产物同步、70 项 Node 测试和 69 项 Playwright 测试全部通过。

## v0.8 补丁四：工作记录编辑器 Markdown 形态与拖入附件入库（deepseek/test-feedback-fixes）

### 本次修复

- 修复 TEST-V08-014：长期描述与每日记录编辑器从 Milkdown Crepe 所见即所得改为 Markdown 源码编辑 + 实时渲染预览（宽屏左右分栏、窄屏源码/预览切换，配常用语法工具栏），历史记录从源码文本改为渲染后的 HTML；编辑区“所见即所存”，不再被 Crepe 加载/保存时规范化重排。
- 编辑器内拖入/粘贴文件自动入库为附件：图片插入 `![名称](attachment:ID)` 图片引用，其它文件插入 `[名称](attachment:ID)` 链接；文件写入任务附件分区（tasks/<ID>/attachments/），超过 20 MB 拒绝并提示。旧 Crepe 产生的 blob: 会话链接在渲染时显示为“图片已失效”说明。
- 新增 `attachment:` 引用解析渲染层（marked + DOMPurify + 按附件 ID 缓存的对象 URL），历史记录、编辑器预览与长期描述统一显示真实图片；附件被删除时显示失效占位。
- 附件面板新增“整理内嵌图片”维护动作：把长期描述与工作记录中内嵌的 data: URI 图片提取为附件并替换为 `attachment:` 引用。
- 移除 @milkdown/crepe 与 ProseMirror 依赖（净减 184 个包）：正式构建从 2823 个模块、多个合计约 1.3 MB 的分块，降为 1888 个模块、单一 472 KB（gzip 144 KB）分块。
- 补充修复 TEST-V08-015：编辑器拖放监听原先只挂在源码 textarea 上且未在 dragover 阶段 preventDefault，真实拖拽（尤其拖到宽屏预览区时）不生效；现将监听扩展到整个编辑器宿主并补 dragover 文件交付，新增 drag-active 高亮态。
- 补充修复 TEST-V08-016：已处理折叠区在折叠态仍显示最新 3 条预览，条目少时点击折叠几乎无可见变化；改为折叠态完全收起全部已处理条目（标题保留数量），展开态显示全部，移除“另有 N 项”提示。
- 补充修复 TEST-V08-017：编辑器增加输入法合成保护（composition 期间不保存不预览、合成结束统一提交，保存函数自动推迟），修复拼音中间态被持久化导致的汉字回退；每日记录与长期描述默认纯 Markdown 源码、工具栏“预览/源码”手动切换渲染预览；新增渲染图片放大遮罩（点击/Esc 关闭，带放大光标）；三轨布局与拖拽调宽阈值由 1340px 下调到 1180px，1180-1339 使用图标导航与紧凑任务列表。
- 补充修复 TEST-V08-018：概览表单自动保存时 trim 掉用户刚敲入的行尾空格与回车，随后界面重绘把规范化值写回输入框；说明字段改为仅统一换行符（保存与读取水合均保留用户空白），renderDetail 不再覆盖聚焦中的字段。

### 验证

- `npm run verify` 通过：反馈文档校验、TypeScript、Vite 正式构建、构建产物同步、69 项 Node 测试和 63 项 Playwright 测试全部通过（新增 TEST-V08-014a 源码编辑/历史渲染、014b 拖入图片入库与刷新后显示、014c data: URI 迁移三个用例；两个拖拽模拟用例在本机整轮负载下偶发抖动、单独复跑通过，与本轮改动无关）。
- `npm run visual:baseline` 通过：全部视口零横向溢出，工作区编辑器截图按新源码+预览形态重新采集。

## v0.8 补丁三：含工作记录备份导入的写后校验误判修复（deepseek/test-feedback-fixes）

### 本次修复

- 修复 TEST-V08-013：任何包含工作记录的备份导入都会在写后校验阶段被误判为内容不一致并自动回滚，导入报告停留在 `prepared` 状态。根因是 `parseWorkLog` 总是把值为 `undefined` 的 `conflictOrigin` 作为自有键返回，而备份清单经过 JSON 序列化后会丢弃该键；逐项校验使用的 `stableValue` 把“键存在但值为 undefined”与“键不存在”判为不同内容。修复后 `parseWorkLog` 仅在确有取值时返回 `conflictOrigin`，`stableValue` 忽略值为 `undefined` 的键（与 JSON 语义一致）。
- 新增 e2e 回归用例：在导出备份中注入一条不含 `conflictOrigin` 键的工作记录，走完整导入流程断言“合并已完成并通过写入校验”且记录持久化；该用例在修复前代码上复现自动回滚失败，修复后通过。
- 顺带加固拖拽时序敏感 e2e 用例：`task name, tag, and six-dot handle each start drag` 的拖拽落位与撤销回退断言从默认 5s 超时放宽到 20s，消除整轮负载下的偶发抖动。

### 验证

- `npm run verify` 通过：反馈文档校验、TypeScript、Vite 正式构建、构建产物同步、69 项 Node 测试和 60 项 Playwright 测试全部通过（新增 TEST-V08-013 导入回归用例）。

## v0.8 补丁二：用户直接体验反馈修复（deepseek/test-feedback-fixes）

### 本次修复

- 修复 TEST-V08-011：文件夹头部携带 data-toggle-folder-id 并显示 pointer 光标；点击头部内按钮、菜单和输入控件以外的任何区域（标题、图标、计数、留白）都切换展开/折叠，操作按钮保持独立行为。
- 修复 TEST-V08-012：宽屏常驻第三轨并给 app-shell 增加 grid-template-columns 过渡，工作区平滑展开；打开工作区后表头保留为与两行任务卡对齐的双行表头（任务/操作首行，优先级/截止日期次行），面板增加入场淡入，reduced-motion 自动禁用动画。
- Playwright e2e 改用专用端口 5180 且禁止复用现有服务器：此前 reuseExistingServer 会静默复用本地 5173 开发服务器，测试可能在旧代码上运行。
- 扩展 LOCAL_WORKSPACE_ACCEPTANCE.md：新增 v0.8 合并导入与重复导入的 11 组真实 Windows 目录验收步骤（对应 TEST-V08-007），包含 keep-both 幂等人工复核与 011/012 体验复核。

### 验证

- `npm run verify` 通过：反馈文档校验、TypeScript、Vite 正式构建、构建产物同步、69 项 Node 测试和 59 项 Playwright 测试全部通过（新增文件夹头部点击与表头保留契约断言、双行表头/轨道过渡 e2e 用例）。
- `npm run visual:baseline` 通过：1440×900 工作区截图等待轨道过渡结束采集，双行表头进入基线；全部视口零横向溢出。

## v0.8 补丁：测试反馈流程改进与开放问题修复（deepseek/test-feedback-fixes）

### 本次修复

- 修复 TEST-V08-009：所有 keep-both 分支生成确定性副本 ID 后先检查目标集合；相同 ID 且内容相同视为已应用并跳过，相同 ID 但内容不同生成受内容哈希区分的新冲突 ID。工作记录、任务、文件夹、附件四类实体均有重复 keep-both 幂等测试。
- 修复 TEST-V08-010：手机断点把导入中心来源区改为单列布局，备份文件名独占一行并允许安全换行；视觉基线增加 importSourceName 文本非空、可见、宽度不小于 80px 和移动端不裁剪断言。
- 建立 docs/feedback/ 测试反馈文档体系：每轮不可变快照、问题登记表（registry.json）、JSON Schema 格式契约与零依赖校验脚本（validate:feedback，已纳入 npm run verify）。
- 确立可复现性规则（source_revision 必须是纯提交哈希）、finding ID 引用约定和发布门槛单一归属（registry.json 为唯一权威，ENGINEERING_REVIEW.json 的 definition_of_done 仅作规划目标）。
- TEST-V08-007（真实 Windows 本地目录人工验收）保持开放并移入 pending_verification 区，不阻塞代码修复排期。

### 验证

- `npm run verify` 通过：反馈文档校验、TypeScript、Vite 正式构建、构建产物同步、67 项 Node 测试和 57 项 Playwright 测试全部通过（Node 测试较上一轮 +5，为四类实体 keep-both 幂等与内容变更新 ID 用例）。
- `npm run visual:baseline` 通过：1440×900 与 390×844 导入中心均断言 importSourceName 文本非空、可见、宽度不小于 80px，移动端文件名无裁剪。

## v0.7 本地工作区：存储契约第一阶段（开发中）

- 从已验收的 v0.6 提交创建独立分支 `codex/local-workspace-storage`，保持与 `develop` 分离。
- 新增 `WorkspaceBackend`，统一任务主状态、长期描述、每日工作记录、附件和完整快照操作。
- 保留旧浏览器数据只读迁移层，运行时业务数据统一写入本地目录；旧数据仅在用户主动选择导入时读取一次。
- 界面、编辑器控制器和备份流程改为只依赖存储契约，不再引用 IndexedDB 实现细节。
- 新增可复用浏览器契约测试，覆盖任务保存/删除/恢复、描述、工作记录、附件编辑及快照恢复。
- 固定 Vite 开发与预览端口，避免未来目录句柄权限因浏览器来源变化而失效。
- 完成 `LocalDirectoryBackend`：稳定任务目录、Markdown 工作记录 Front Matter、附件原始副本、回收区和临时文件校验写入。
- 增加目录句柄持久化、启动权限检查、权限重新授权、失效目录改选和明确的工作区状态；目录不可用时不加载浏览器业务数据。
- 工作区与任务 revision 防止静默覆盖外部修改；Web Locks 串行化同源写入，BroadcastChannel 通知其它标签页刷新。
- 文本附件支持浏览器内编辑保存；HTML/SVG 强制按文本处理，不作为主动内容执行。
- `IndexedDbBackend` 与 `LocalDirectoryBackend` 均通过同一套存储契约测试。
- 根据第 8 版测试反馈修复任务删除后的撤销语义：重新出现的 taskId 会优先从 `trash` 恢复完整目录，描述、工作记录、附件与哈希一起恢复；删除先等待后端成功，撤销/重做等待落盘后再提示完成。
- 为描述、每日记录和附件增加 SHA-256 内容基线，写前重新读取磁盘并阻止静默覆盖；界面提供重新加载外部版本、保存当前编辑为冲突副本、取消保存三个处理选项。
- 迁移使用未完成标记，先恢复任务资料并逐项校验数量与内容哈希，最后发布 `workspace.json`；首次迁移失败清理半成品，替换已有工作区失败则完整回滚。
- 临时文件和最终文件均校验 SHA-256，最终写入失败时尝试恢复原文件；权限拒绝、配额/写入失败保持最后的持久化内容。
- 1024px 的 72px 导航栏将工作区入口收敛为 40×40px 图标按钮和状态点，完整文字保留在可访问名称及 tooltip 中。
- 工作区标签切换会先刷新长期描述和每日记录的自动保存，避免快速切换后立即删除或刷新造成未落盘内容丢失。
- 根据第 9 版测试反馈拆分本地目录恢复操作：权限撤销可“重新授权原目录”，目录移动、删除或句柄失效时可“改选本地目录”；不可恢复句柄不再被反复重试，旧浏览器数据不会自动成为运行时回退源。
- 加入损坏工作区保护：只有真正空目录可初始化；非空目录缺少 `workspace.json` 时拒绝读取和写入，避免用默认任务覆盖仍在 `tasks`、`trash` 或其它残留文件中的资料。
- 根据第 11 版测试反馈修复真实 Windows 目录迁移：临时文件改用不以点号开头的 `tmp-<uuid>-<safe-name>` 名称，并统一校验 Windows 保留名、路径分隔符和尾随点/空格；清理失败会保留原始写入错误并追加诊断信息。

### 本轮验证

- 本地目录专项浏览器测试覆盖完整删除/撤销/刷新、三类外部内容冲突、三种冲突处理、首次迁移中途失败、已有工作区回滚、权限拒绝、配额写入失败、索引缺失写入保护、失效目录改选和 Windows 安全临时文件名清理。
- `npm run verify` 通过：TypeScript、Vite 正式构建、构建产物同步、48 项 Node 测试和 51 项 Chromium 端到端测试全部通过。
- `npm run visual:baseline` 通过：1440×900、1024×768、768×1024 与 390×844 均无横向溢出；1024px 图标导航的工作区文字全部隐藏，按钮固定为 40×40px。
- 附件安全回归覆盖 PDF 专用预览，以及 HTML/SVG 仅以纯文本显示且不提供主动打开入口。
- 1024×768 自动断言工作区状态文字隐藏、入口按钮固定为 40×40px，并保留可访问名称。
- `LOCAL_WORKSPACE_ACCEPTANCE.md` 记录真实 Chrome/Edge 目录的权限撤销、目录移动、迁移中断、外部修改、磁盘失败与附件安全手工验收步骤，明确区别于 OPFS 自动化。

## v0.6 视觉重构：改进建议第 4 版

- 样式由根目录单文件迁移至 `src/styles/` 六模块，由 Vue 入口交给 Vite 统一构建；静态组件补充 scoped 局部规则。
- 宽屏三栏固定使用约 232px 导航和 480-680px 可调工作区，1024-1180px 标准桌面切换 72px 图标导航，平板和手机保持分层交互。
- 高、低优先级选中态分别使用暖色与冷色边框、2px 内描边及左侧强调，不再混入主青色选中描边。
- reduced-motion 全局关闭非必要动画，并将 768x1024、系统深色和减少动效纳入浏览器验收矩阵。

### 验证

- `npm run verify` 通过：TypeScript、Vite 正式构建、构建产物同步、45 项 Node 测试和 34 项 Playwright 测试全部通过。
- `npm run visual:baseline` 通过：1440×900、1024×768、768×1024 与 390×844 正式构建基线均无横向溢出；标准桌面和平板常规任务行最高 68px，宽屏工作区任务行最高 87px。

## 第 5 轮补丁二：第 6 版测试反馈修复

### 本次修复

- 高、低优先级分别增加同色系悬停与选中颜色；选中态通过暖色/冷色背景、优先级强调边和内描边共同表达，不再统一覆盖为青蓝色。
- 任务行改为默认选择目标，标题、标签、优先级、截止日期、卡片留白及操作按钮之间的空隙均可选中任务；真实按钮、表单、链接和拖拽控件继续保持独立行为。
- 1440px 宽屏打开工作区后采用专用两行紧凑布局：首行集中标题与操作，次行集中标签、优先级和日期；560px、620px、680px 三档工作区宽度下任务卡高度均不超过 112px。
- 拖拽期间创建与源任务等高的临时占位，落点变化时在松手前实时预排列；相邻任务使用 190ms FLIP 位移动画连续让位，减少动态效果模式下直接换位。
- 拖拽预览只操作临时 DOM，松手后一次性写入任务移动历史；无效落点完整恢复占位、源任务和原顺序，跨折叠文件夹仍支持延时展开和落点保持。
- 高低优先级分界改为 28×32px 暖/冷双色阈值标记和横向拖动图标，保留 44×40px 命中区、提示文字及键盘操作。
- 移除所有文件夹标题下方横线，改用留白和轻底色组织标题；工作区调宽轨道改为常驻低对比度显示，悬停和聚焦时增强。

### 验证

- `npm run verify` 通过：TypeScript、Vite 正式构建、构建产物同步、43 项 Node 测试和 32 项 Playwright 测试全部通过。
- 新增浏览器验收覆盖双色选中态、整行点击、三档工作区宽度、文件夹无横线、分界与调宽轨道、实时占位、FLIP 动画、无效落点恢复和减少动态效果。
- 正式构建在 1440×900 深色主题下复核：任务卡高度约 86.3px，操作区后留白约 10.7px，工作区宽 620px，文件夹标题边框均为 `none`。
- 正式构建中高优先级选中背景为暖色 `rgb(80, 62, 43)`，低优先级为冷色 `rgb(48, 74, 84)`，选中后仍能辨认优先级归属。

## 第 5 轮补丁：最新测试反馈修复

### 本次修复

- 修复任务工作区在分栏、替换和手机全屏三种模式间切换时的瞬时位移，1440×900、1366×768、1180×800、1024×768 与 390×844 均保持任务区和工作区零重叠。
- 将高/低优先级改为暖色与冷色整行分区，移除重复分组标题和横向分隔线；保留紧凑的左侧分界手柄，并提供不小于 40×40px 的拖动与键盘操作区域。
- 四层文件夹按深度显示不同的便签底色、边框和轻阴影，深色主题及等效 150% 缩放下仍保留父级边缘且无横向溢出。
- 工作记录补齐新建、编辑、删除和 8 秒原位撤销；删除后可恢复原日期、进度和 Markdown 内容。
- 任务树文件夹标题增加可发现的操作菜单；移动时可精确选择父文件夹和同级位置，删除前展示受影响的直属子文件夹与任务数量。
- Markdown 编辑器改为按需启用功能与样式，移除未使用的代码语言包、公式与 KaTeX 字体资源，正式构建不再产生超大分块警告。
- 扩展真实浏览器验收，覆盖任务标题、标签和拖动手柄拖拽、分界线键盘操作、文件夹移动删除撤销、工作记录完整增删改撤销及五档工作区布局。

### 验证

- `npm run verify` 通过：TypeScript、Vite 正式构建、构建产物同步、41 项 Node 测试和 27 项 Playwright 测试全部通过。
- 最大 JavaScript 分块约 322 KB（gzip 约 97 KB），构建产物中不包含 KaTeX 字体和 CodeMirror 语言模块。

## 第 5 轮：任务工作区与本地资料库

### 本轮完成

- 升级到 schema v5，任务增加长期 Markdown 描述，工作区宽度进入偏好；v4 任务无损迁移且旧 JSON 备份继续可导入。
- 新增概览、工作记录、附件三个任务工作区标签；宽屏支持 560-680px 拖动和键盘调宽，常规桌面覆盖显示，手机使用全屏工作区。
- 接入 Milkdown Crepe 所见即所得 Markdown 编辑器，长期描述和每日记录具有独立的 700ms 自动保存、立即保存、错误重试与源码预览后备模式。
- 新增按任务和日期唯一的工作记录、进度百分比、历史日期编辑、未来日期阻止、倒序历史及完成/丢弃只读语义。
- 新增 IndexedDB `workLogs`、`attachments`、`attachmentBlobs` 存储，支持文本/图片/PDF 预览、常见文件打开导出、重命名、图片引用和容量提示。
- 完整备份升级为 ZIP：`manifest.json` 保存应用状态和索引，工作记录以 Markdown 保存，附件保留原始二进制；导入在替换前完成结构和文件完整性校验。
- 任务树按桌面 22px、移动端 16px 的分支层级缩进并显示引导线；任务正文成为主拖拽区，拖拽后的误点击被抑制。
- 深色主题统一为中性炭灰表面层级，Milkdown 与应用共享主题变量；DOMPurify 升级到已修复安全公告的版本。

### 验证

- TypeScript 严格类型检查、Vite 正式构建、构建产物同步检查和依赖安全审计通过。
- Node 测试覆盖 schema v5 迁移、存储 fallback、工作区契约、IndexedDB 与 ZIP 备份结构。
- Playwright 覆盖 Markdown 自动保存、切换前落盘、完成态只读、附件 Blob 与完整备份恢复、宽度记忆、手机全屏、四层树等效 150% 缩放和深色对比度。
- 1440×900 与 390×844 实际截图复核无重叠；移动端概览字段独立滚动，保存操作区固定在底部。

## 第 4 轮：任务树编排升级

### 本轮完成

- 升级到 schema v4，优先级收敛为高/低两级，v3 的中/低优先级稳定迁移为低优先级。
- 引入 Pragmatic Drag and Drop 与边缘自动滚动，支持任务同区排序、跨文件夹移动、折叠文件夹悬停展开和优先级分界线拖动。
- 在根级列表末尾和文件夹标题提供任务/文件夹行内创建，侧栏收敛为全局新建入口与紧凑默认值；支持 Enter 保存和 Escape 取消。
- 完成/丢弃进入 8 秒持久化待决状态，可独立撤销；到期后按直属文件夹进入虚拟“已处理”区。
- 逾期任务独立置顶，支持未来日期快捷改期、自定义原因和详情改期时间线，改期与时间线一起撤销/重做。
- 移除独立排序控件，改为任务树、全局优先级、全局日期、优先级＋日期四种互斥视图，并支持全局任务定位回任务树。
- 全面采用 Lucide 图标，补齐工具提示、ARIA 名称、实时播报、键盘等价操作和手机 44px 触控目标。
- 浏览器入口使用 esbuild 打包外部依赖，同时保留 TypeScript 模块产物供 Node 测试直接覆盖。
- 根据浏览器反馈修复待办筛选中的待决任务消失、已处理区早于子文件夹、空改期时间线、逾期操作拥挤及小尺寸图标按钮问题。
- 折叠文件夹拖拽改为临时展开内容，离开后恢复折叠，放入目标后保持展开；自放置不会产生撤销历史。

### 验证

- TypeScript 严格类型检查通过。
- Node 测试：37 项通过，覆盖 Vite 开发链、v4 迁移、待决筛选语义、原子移动、分界线、独立计时、改期历史和存储 fallback。
- Playwright：18 项真实浏览器测试覆盖回车/取消创建、待办筛选双计时器、已处理排序、逾期更多操作、折叠悬停、精确拖放、分界线持久化和桌面/手机最终尺寸。
- 1440×900 实测任务名称区宽约 715px，标题保持单行，页面无横向溢出。

## 第 3 轮补丁二：桌面验收修复

### 本次修复

- 1200-1500px 统一使用完整任务区和详情抽屉，消除 1280/1281px 断点处任务行突然缩窄的问题。
- 宽屏未选择任务时不再保留空详情列；选中任务后才显示第三列，并提供关闭入口。
- 应用外壳固定为 `100dvh`，导航、任务列表和详情正文独立滚动；详情操作区固定在底部。
- 截止日期明确区分“逾期”“今天截止”“未来日期”和“无截止日期”，概览标签改为“今日/逾期”。
- 排序文案从“手动顺序”改为“添加顺序”，准确表达当前按任务加入顺序排列的行为。
- 统一“新建任务”和“重置为示例数据”文案，删除重复状态说明，并将重要辅助文字提升到至少 12px。

### 验证

- Node 测试：29 项通过，0 项失败。
- Playwright：10 项真实浏览器测试通过，0 项失败。
- 浏览器复核：1366×768 抽屉详情和 1920×768 三栏详情无内容遮挡，底部操作始终可见。
- `npm run verify`、`git diff --check`、TypeScript 类型检查和构建产物同步检查均通过。

## 第 3 轮补丁：测试反馈修复

### 本次修复

- 根据 `TEST_FEEDBACK.json` 将 760px 及以下任务行操作按钮的触控区域从 28×28px 扩大到 44×44px。
- 新增真实浏览器端到端测试，覆盖详情保存与撤销、状态流转、文件夹创建删除和三种视图切换。
- 新增 1440×900 与 390×844 视口回归，检查页面和任务行横向溢出，并直接测量手机端操作按钮尺寸。
- 使用 Node 静态服务统一本地预览和自动化测试启动方式，避免依赖 Python 环境。
- 新增 GitHub Actions 验证工作流，持续集成会安装 Chromium 并执行完整 `npm run verify`。

### 验证

- `npm run verify` 通过。
- Node 测试：27 项通过，0 项失败。
- Playwright：6 项真实浏览器测试通过，0 项失败。
- `git diff --check`、TypeScript 类型检查、构建产物语法与同步检查均通过。

## 第 3 轮：纯任务管理器

### 本轮完成

- 移除改进记录、下一轮计划、产品反馈、开发历史和轮次归档等开发流程界面。
- 升级到 schema v3：任务增加说明、三态状态、文件夹引用和手动顺序；新增文件夹与视图偏好实体。
- 支持 v1/v2 本地数据和备份迁移；当前 key 损坏时继续尝试旧版本数据。
- 新增四层文件夹树、循环防护、折叠持久化、移动和两种非空删除策略。
- 新增树状、优先级、截止日期视图，以及共享搜索、状态筛选、文件夹范围和稳定排序。
- 用行选中与响应式详情面板替代编辑按钮和编辑弹窗，并增加未保存切换保护。
- 新增完成、不再需要、恢复、优先级升降和删除行内操作；边界 no-op 不进入撤销历史。
- 修复空状态显示和表头错位，并完成桌面、平板、手机响应式重构。

### 验证

- `npm test`：27 项测试通过。
- Playwright：1440、1024、768、390px 四档布局和关键交互通过。
- 验证项目包括列对齐、无横向溢出、详情保存/取消、未保存提示、三种视图、行内操作隔离和控制台错误。

## 第 1 轮：基础可用版

### 本轮完成

- 创建本地静态 Web 应用结构：`index.html`、`styles.css`、`app.js`。
- 实现任务新增、编辑、删除、完成状态切换。
- 实现搜索、状态筛选、优先级排序和概览统计。
- 使用浏览器 `localStorage` 保存数据。
- 实现本轮完成记录、下一轮建议、反馈输入、迭代历史归档。
- 增加数据导出功能。

### 遗留问题

- `app.js` 是单文件巨石，状态、UI、存储和业务逻辑耦合。
- 缺少类型约束、数据校验和测试。
- 使用原生 `prompt` / `confirm`。
- 任务列表全量重建 DOM。
- 只支持导出，不支持导入。

## 第 2 轮：结构优化版

### 本轮完成

- 将开发分支推进到 `feature/iteration-2`，针对结构问题进行重构。
- 新增 TypeScript 源码结构：`src/types.ts`、`src/domain.ts`、`src/store.ts`、`src/storage.ts`、`src/ui/*`、`src/main.ts`。
- 把状态管理改为 reducer + store，避免业务逻辑直接散落在全局可变单例里。
- 增加 schema 版本、数据恢复、旧字段迁移和导入文件校验。
- 将任务编辑、确认、添加改进项改为页面内 `dialog`，替代原生 `prompt` / `confirm`。
- 历史记录渲染改为 DOM API 逐节点创建，不再拼接 `innerHTML`。
- 任务列表渲染改为 keyed reconcile，避免每次操作都重建整棵任务列表。
- 增加完整任务编辑：内容、优先级、截止日期、标签都可修改。
- 增加导入备份恢复、撤销/重做、到期/逾期统计、系统/浅色/深色主题。
- 将“计划/迭代/反馈”语言收敛为“任务/改进项/产品反馈”，减少概念混用。
- 新增 Node 测试，覆盖数据恢复、反馈建议、任务筛选、reducer、撤销/重做、导入解析。

### 验证

- 已被工程审核补丁替换为 `npm run verify`。
- 待运行：`npm test`
- 当前环境未安装 `tsc`，`npm run typecheck` 需要先安装依赖。

### 仍未彻底解决

- TypeScript 源码已建立；工程审核补丁已移除手工维护的 `dist/app.js`。
- 反馈建议仍是规则匹配，只是从硬编码散落逻辑收敛为领域层规则表；真正智能化需要接入模型或可配置规则。
- `localStorage` 仍有容量上限；大规模数据应迁移到 IndexedDB。
- 尚未实现 PWA 离线安装。
- 没有虚拟滚动，超大任务列表仍需要进一步优化。

### 工程审核反馈

- 代码审核发现构建链、测试目标、存储异常、导入校验、撤销历史和日期处理存在工程风险。
- 详细问题、优先级和验收标准见 `ENGINEERING_REVIEW.json`。

### 下一轮建议与计划

- 评估是否引入 Vite 或其他开发服务器，改善 ES Modules 本地预览体验。
- 增加 IndexedDB 存储层，并保留 `localStorage` 作为轻量兼容 fallback。
- 将反馈规则改为可配置数据文件，或预留 AI 建议接口。
- 增加 PWA manifest、service worker 和离线安装能力。
- 增加子任务、批量操作和标签统计视图。

## 第 2 轮补丁：工程审核修复

### 本次修复

- 建立可复现构建链：`npm run build` 会清理并由 `src/` 自动生成 `dist/`，`dist/app.js` 手工包已移除。
- 提交 `package-lock.json`，确保安装依赖后 `npm run typecheck` 可复现。
- 将 HTML 入口改为 `dist/main.js` ES Module，运行产物来自 TypeScript 编译输出。
- 修复 `localStorage` 读写异常处理：存储不可用或容量写入失败时，应用继续使用内存状态并提示用户。
- 加强导入备份校验：无关 JSON、数组、缺少核心字段的对象不会被当作有效备份。
- 明确导入语义：导入是确认替换数据，不进入普通撤销历史，执行后清空 undo/redo 栈。
- 修复 reducer no-op：目标 id 不存在或输入无实际变化时返回原 state，不污染渲染和撤销历史。
- 修复日期逻辑：`toISODate` 改为基于本地年月日格式化，避免 UTC 截断导致错日。
- 调整测试，不再直接 mutation 默认 state，并补充 review 要求的回归场景。

### 验证

- `npm run typecheck` 通过。
- `npm test` 通过，13 项测试全部通过。
- `npm run verify` 通过。
- `node --check dist/main.js`、`dist/domain.js`、`dist/store.js`、`dist/storage.js` 通过。

### 残留风险

- 当前 HTML 使用 ES Modules，部分浏览器直接通过 `file://` 打开时可能限制模块加载；建议用本地静态服务器访问。
- `dist/` 仍被提交用于开箱运行，但已经由 `src/` 自动生成，不再手工维护。
- 反馈建议仍是规则匹配，尚未接入模型或可配置规则。

## 第 2 轮补丁：复审构建修复

### 本次修复

- README 删除“直接打开 `index.html`”说法，统一为 `npm run build` 后通过 `npm run serve` 本地静态服务器运行。
- 新增 `serve` 脚本，降低 ES Modules 本地预览门槛。
- 新增 `check:dist` 脚本，`verify` 会在构建和测试后执行 `git diff --exit-code -- dist`，防止提交的 `dist/` 与 `src/` 不同步。
- 修复备份校验假值漏洞：`tasks` 中包含 `null`、数字、布尔值等无效项时会拒绝导入。
- 明确导入迁移语义：当前 v2 备份和 legacy v1 备份分支校验，旧备份可以按声明迁移。
- 修复本地存储中 `{}`、`[]` 等可解析垃圾 JSON 被误提示恢复成功的问题。
- 对存储失败 toast 做消息记忆，避免同类存储失败在每次状态变化时重复提示。

### 验证

- `npm test` 通过，18 项测试全部通过。
- `npm run verify` 需要在构建产物暂存后执行，因为它会检查 `dist/` 是否与仓库提交同步。
