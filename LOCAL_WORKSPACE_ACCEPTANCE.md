# 本地工作区真实目录验收清单

本清单用于 Chrome 或 Edge 的真实操作系统目录验收，补充自动化中的 OPFS 后端契约和故障注入。每次准备验收 v0.7 时，在一个空测试目录和一个已有工作区副本上各执行一次；不要使用唯一的数据副本。

## 环境

- 使用 `http://127.0.0.1:5173/`，确认地址和端口没有变化。
- 准备空目录 A、已有工作区副本 B，以及可编辑 Markdown 和二进制文件的外部程序。
- 在浏览器开发者工具中确认页面没有未处理异常。

## 权限与目录状态

- 选择目录 A 并授权读写，刷新后应继续打开同一工作区。
- 在浏览器站点设置中撤销目录权限，刷新后应只显示“重新授权原目录”和“选择本地目录”；重新授权后恢复原目录工作区，不得加载浏览器业务数据。
- 关闭页面后移动或重命名目录，再次打开时不得静默创建空工作区；应显示“改选本地目录”，且反复点击不会继续请求已失效的旧句柄。
- 在工作区副本中移走 `workspace.json` 并保留 `tasks` 或 `trash`，刷新后应提示索引缺失并阻止读取和写入；恢复索引或改选正确目录前，不得在该目录创建新索引或改写任务资料。
- 选择真正的空目录仍应显示创建确认；旧浏览器数据只能在用户主动选择“从旧数据导入”时一次性迁移。
- 让目录或单个文件变为只读，保存应显示错误，编辑器当前内容保持可重试。

## 迁移与回滚

- 向空目录 A 迁移任务、描述、每日记录和附件；迁移完成前不应出现可正常打开的 `workspace.json`。
- 迁移完成后逐项核对任务数量、记录数量、附件名称、大小和内容，再刷新浏览器复核。
- 用真实 Windows 目录检查迁移期间出现的临时文件均使用普通 `tmp-...` 名称，不以点号开头；迁移完成后目录中不得残留 `tmp-*` 文件，也不得出现 `Name is not allowed`。
- 使用目录 B 的副本测试替换迁移，故意在迁移时撤销权限或使目录只读；失败后重新打开 B，应仍是迁移前的完整内容。
- 失败目录中不得残留可被正常打开的半成品；若存在未完成标记，应用必须拒绝正常打开。

## 外部修改冲突

- 浏览器打开长期描述后，用外部编辑器修改 `description.md`；浏览器下次保存必须出现冲突对话框，不能覆盖外部版本。
- 对 `worklogs/YYYY-MM-DD.md` 和文本附件重复上述操作。
- 分别验证“重新加载外部版本”“保存当前内容为冲突副本”“取消保存”；冲突副本应位于对应任务目录且内容完整。
- 用外部程序替换同大小的附件，浏览器仍应通过 SHA-256 检测到冲突。

## 删除、撤销与写入失败

- 为任务添加描述、记录和附件，删除后确认目录进入 `trash/<task-id>/`。
- 点击撤销并等待“撤销已保存”，刷新后核对任务及全部资料；`trash` 不应残留同 taskId。
- 在磁盘空间不足或受控只读卷上触发保存失败，确认最后一次成功保存的文件仍可打开，当前编辑内容留在页面中。
- 恢复磁盘条件后重试保存，刷新并核对内容哈希一致。

## 附件安全与视觉

- 图片和 PDF 使用应用内预览；HTML 与 SVG 仅显示源码文本，不执行脚本，也不提供浏览器主动打开入口。
- 在 1024x768 检查 72px 导航栏：工作区只显示状态点和 40x40px 图标按钮，完整名称通过悬停提示和辅助技术读取。
- 在 1440x900、768x1024 和 390x844 复核任务区、工作区与操作按钮无重叠或横向溢出。

## v0.8 合并导入与重复导入验收（对应 TEST-V08-007）

使用 Chrome 或 Edge 打开 `http://127.0.0.1:5173/`，准备空目录 A、已有工作区副本 B（不要使用唯一数据副本），以及外部 Markdown 编辑器和文件管理器。

准备材料：

- 在 B 中导出 v6 备份 `current.zip`。
- 用外部编辑器修改 B 中某任务的 `tasks/<task-id>/worklogs/YYYY-MM-DD.md`（模拟另一设备修改），再导出第二份 `incoming.zip`。
- 如需验证 v5 兼容路径，另备一份 v5 格式 ZIP 或旧 JSON 备份。

### 1. 导入中心预览

- 点击“导入或合并备份”选择 `incoming.zip`：来源区域显示文件名、v6 标识、导出时间和来源工作区摘要。
- 默认筛选“待处理”；逐项显示类型、差异摘要、处理方式下拉。
- 修改任意冲突项的决定后，顶部统计（将新增/将更新/保留副本/跳过）随之更新。

### 2. 工作记录冲突保留双方

- 对冲突工作记录选择“两者都保留”并应用合并。
- `tasks/<task-id>/worklogs/` 下出现同日两个独立文件（原文件与 `worklog-imported-*` 冲突副本），原文件未被覆盖。
- 应用后无回滚提示，结果报告中记录总数 +1。

### 3. 附件冲突保留双方

- 对同 ID 不同内容的附件选择“两者都保留”：`attachments/` 下出现两个文件，各自的 SHA-256 可独立验证。

### 4. 重复导入幂等（TEST-V08-009 人工复核）

- 以第一次合并结果为当前工作区，再次分析同一 `incoming.zip`，对同一冲突再次选择“两者都保留”：应用后实体总数不变，不出现重复 ID，不报写后校验失败。
- 换一份内容再次变化的备份并保留双方：生成带内容哈希后缀的新冲突副本，两份副本可同时打开且内容互不覆盖。

### 5. 导入恢复记录与回滚

- 导入前工作区自动保存 `latest-before-import.zip`；“最近一次导入”可查看报告、定位受影响任务、下载报告与一键回滚。
- 回滚后刷新：任务数、工作记录数、附件名称与大小和导入前一致；`trash/` 不残留同 taskId。

### 6. 权限撤销与失效目录

- 浏览器站点设置中撤销目录权限并刷新：只显示“重新授权原目录”和“选择本地目录”；重新授权后恢复原工作区，不得加载浏览器业务数据。
- 关闭页面后移动或重命名目录再打开：提示“改选本地目录”，不会静默创建空工作区。

### 7. 写入中断与失败回滚

- 应用合并前将工作区目录或 `workspace.json` 设为只读（或使用受控只读卷），执行应用合并：显示错误并保留导入前内容，编辑器内容可重试。
- 若触发失败回滚，重新打开 B 后仍是导入前的完整内容；恢复权限后可重新导入成功。

### 8. 杀毒与同步目录

- 把 `incoming.zip` 放入 OneDrive/Dropbox 等同步目录后再导入：不因文件占用报错，结果与普通目录一致。

### 9. 重新打开核验

- 关闭浏览器并重新授权目录：任务数、工作记录数、附件名称/大小/内容哈希与导入后一致，`tmp-*` 无残留。

### 10. 手机视口复核（TEST-V08-010）

- 窗口缩到 390x844 打开导入中心：备份文件名独占一行完整可见，长文件名允许换行，不出现被压缩到不可见的情况。

### 11. 交互体验复核（TEST-V08-011 / TEST-V08-012）

- 点击文件夹标题的文字、图标或空白处即可展开/折叠；点击头部内的“新建任务”“新建子文件夹”“管理”按钮不会误触折叠。
- 1440px 选中任务：表头“任务/优先级/截止日期/操作”保留为双行表头并与任务卡对齐，工作区平滑展开；系统开启“减少动态效果”后无动画直接到位。
