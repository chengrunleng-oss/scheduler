# 本地工作区真实目录验收清单

本清单用于 Chrome 或 Edge 的真实操作系统目录验收，补充自动化中的 OPFS 后端契约和故障注入。每次准备验收 v0.7 时，在一个空测试目录和一个已有工作区副本上各执行一次；不要使用唯一的数据副本。

## 环境

- 使用 `http://127.0.0.1:5173/`，确认地址和端口没有变化。
- 准备空目录 A、已有工作区副本 B，以及可编辑 Markdown 和二进制文件的外部程序。
- 在浏览器开发者工具中确认页面没有未处理异常。

## 权限与目录状态

- 选择目录 A 并授权读写，刷新后应继续打开同一工作区。
- 在浏览器站点设置中撤销目录权限，刷新后应回退浏览器存储并同时提供“重新授权原目录”和“选择本地目录”；重新授权后恢复原目录工作区。
- 关闭页面后移动或重命名目录，再次打开时不得静默创建空工作区；应回退浏览器存储、显示“改选本地目录”，且反复点击不会继续请求已失效的旧句柄。
- 在工作区副本中移走 `workspace.json` 并保留 `tasks` 或 `trash`，刷新后应提示索引缺失并回退浏览器存储；恢复索引或改选正确目录前，不得在该目录创建新索引或改写任务资料。
- 选择真正的空目录仍应显示创建确认，并可完整迁移当前浏览器任务资料。
- 让目录或单个文件变为只读，保存应显示错误，编辑器当前内容保持可重试。

## 迁移与回滚

- 向空目录 A 迁移任务、描述、每日记录和附件；迁移完成前不应出现可正常打开的 `workspace.json`。
- 迁移完成后逐项核对任务数量、记录数量、附件名称、大小和内容，再刷新浏览器复核。
- 用真实 Windows 目录检查迁移期间出现的临时文件均使用普通 `tmp-...` 名称，不以点号开头；迁移完成后目录中不得残留 `tmp-*` 文件，也不得出现 `Name is not allowed`。
- 使用目录 B 的副本测试替换迁移，故意在迁移时撤销权限或使目录只读；失败后重新打开 B，应仍是迁移前的完整内容。
- 失败目录中不得残留可被正常打开的半成品；若存在未完成标记，应用必须拒绝正常打开。

## 外部修改冲突

- 浏览器打开长期描述后，用外部编辑器修改 `description.md`；浏览器下次保存必须出现冲突对话框，不能覆盖外部版本。
- 对 `worklogs/YYYY-MM-DD.md` 和文本附件重复上述操作。
- 分别验证“重新加载外部版本”“保存当前内容为冲突副本”“取消保存”；冲突副本应位于对应任务目录且内容完整。
- 用外部程序替换同大小的附件，浏览器仍应通过 SHA-256 检测到冲突。

## 删除、撤销与写入失败

- 为任务添加描述、记录和附件，删除后确认目录进入 `trash/<task-id>/`。
- 点击撤销并等待“撤销已保存”，刷新后核对任务及全部资料；`trash` 不应残留同 taskId。
- 在磁盘空间不足或受控只读卷上触发保存失败，确认最后一次成功保存的文件仍可打开，当前编辑内容留在页面中。
- 恢复磁盘条件后重试保存，刷新并核对内容哈希一致。

## 附件安全与视觉

- 图片和 PDF 使用应用内预览；HTML 与 SVG 仅显示源码文本，不执行脚本，也不提供浏览器主动打开入口。
- 在 1024x768 检查 72px 导航栏：工作区只显示状态点和 40x40px 图标按钮，完整名称通过悬停提示和辅助技术读取。
- 在 1440x900、768x1024 和 390x844 复核任务区、工作区与操作按钮无重叠或横向溢出。
